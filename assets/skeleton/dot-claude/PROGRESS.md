# PROGRESS (append-only, honest)

Per entry: date · scope · works / stubbed / next. Appended at the **Record** step of every slice, in
the journal commit alongside STATE.md + TASKS.md + the ledger. Never rewritten — orient reads only the
tail after STATE.md's Progress cursor.

- **{{DATE}} — setup.** works: drop-in `.claude/` harness (constitution, WORKFLOW, hooks, settings, manifests, ledger, state docs, scripts, git hooks). stubbed: none. next: `/start` → open shift s-001 → M1 ({{KEYSTONE}}).
